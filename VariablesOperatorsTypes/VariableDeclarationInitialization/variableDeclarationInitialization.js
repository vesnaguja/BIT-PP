//DOMAĆI Variable declaration and initialization:

/*ZADATAK 1. . Create several variables using different letter cases and beginning characters. Check if they still exist if you refer to them in different case - does case matter here? Try to declare a variable starting with number as the first character.*/
/*Ako se promeni slovo npr. veliko umesto malog, tretiraće to kao novu varijablu i u consoli će pokazivati da varijabla nije definisana.
  Ako varijablu počnem brojem, pokazaće sintaxnu grešku*/

      var ime = 'Vesna';
      console.log(ime);

/*ZADATAK 2. Save a string (text) describing your current mood in a variable and print it out in console.*/

     var mood = 'happy';
     console.log(mood);
     



